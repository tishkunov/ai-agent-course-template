# Research report: <подсистема / вопрос>

> Выход `research` / `researcher`. Анализ уже собранного кода перед реализацией — «как это устроено сейчас».
> Не путать с `requirements.md` (бизнес-требования, агент `analyst`).

## Область и вопрос
Что именно разбираем и зачем (какой пункт M.x предстоит).

## Структура / модули
Какие пакеты `app/*` задействованы, границы слоёв, точки входа.

## Модель данных (актуальная)
Таблицы и поля, которые трогает будущая работа; текущее состояние `requests` / `llm_calls` / `messages` / `State`.

## Ключевые потоки
Пошагово: какой код за какой шаг S1–S10 отвечает; где вызывается `LLMClient`, retrieval, инструменты.

## Паттерны и договорённости
Как в этой базе принято: обработка ошибок, сессии БД, версионирование промптов, моки в тестах.

## Известные слабые места / долги
Из `decisions.md` (известные ограничения), TODO в коде, места без тестов.

## Риски для предстоящей работы
Что может сломаться ниже по цепочке; какие зафиксированные контракты (`ai_context/3-priyomka/11`) рядом.
